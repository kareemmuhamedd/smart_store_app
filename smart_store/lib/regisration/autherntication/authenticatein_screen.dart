import 'package:flutter/material.dart';

class AuthenticatingScreen extends StatelessWidget {
  const AuthenticatingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Container(
      child: Text('authenticating screen'),
    );
  }
}
